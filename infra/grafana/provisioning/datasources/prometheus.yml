# ============================================================
# Grafana Datasource — tells Grafana where Prometheus is
# This file is auto-loaded when Grafana starts
# ============================================================
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true